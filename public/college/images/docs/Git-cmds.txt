////////////////Git commands////////////////

git clone <---clone file link--->
git status
git admin*
git config --global user.name "Your Name Comes Here"
git config --global user.email you@yourdomain.example.com
<----add your github credential and login------->
check status again

Now for checking purpose add some file eg:aish_demo

To add this file to ur clone use push
git push -u origin master

To delete, just delete it from project folder

To pull use
git pull

///////////////Upload the folder online on git//////////
git init
git status
git add<space>.
git commit -m "Your message"
git remote add origin https://github.com/aishwaryanc/resiportary name.git
git push -u origin master
To delete your uploaded resiportary------>Goto setting there add resiportary name and delete
